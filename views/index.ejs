<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles.css" />
    <title>My Weather</title>
  </head>
  <body class="bg-red-50">
    <!-- header -->
    <header class="flex justify-center items-center h-28 bg-red-200">
      <h1 class="text-white text-5xl font-extralight uppercase">my Weather</h1>
    </header>

    <!-- display -->
    <main class="text-center my-16">
      <div>
        <form action="/" method="post" class="flex justify-center flex-col items-center md:flex-row">
          <input type="text" class="w-72 p-3 rounded-lg shadow-lg md:w-80 md:h-16" placeholder="Location" id="location" name="location" autocomplete="off" />
          <!--btn h-16 w-28 ml-6 , inp h-16 w-96 -->
          <button type="submit" class="w-72 shadow-lg btn-red my-2 md:w-28 md:h-16 md:mx-4">Search</button>
        </form>
      </div>

      <div class="text-center">
        <div class="bg-gray-50 w-72 min-w-max mx-auto shadow-lg my-16 rounded-lg p-6 flex flex-col justify-around relative md:flex-row md:h-64 sm:w-80">
          <!-- weather Icon -->
          <div class="flex items-center w-32 mx-auto md:mx-2.5">
            <img src=" http://openweathermap.org/img/wn/<%= icon %>@4x.png" alt="" />
          </div>

          <div class="flex flex-col items-start justify-center">
            <div class="flex justify-center">
              <p class="text-5xl font-light"><%= Math.round(temp) %></p>
              <p class="text-lg font-extralight">˚C</p>
            </div>
            <label for="location" class="text-4xl font-extralight text-gray-400 my-1"><%= cityName %></label>
            <p class="text-lg font-extralight"><%= description %></p>
          </div>
        </div>
      </div>
    </main>

    <!-- footer -->
    <footer></footer>
  </body>
</html>
